// const path = require('path')

const config = {
    cwd: __dirname,
    sass: {
        dist: {
            files: [
                { expand: true, cwd: 'scss', src: ['bootstrap.scss'], dest: '../../../vendor/bower-asset/src/css', ext: '.css' }
            ]
        }
    }
}

module.exports = function (grunt) {

    grunt.initConfig({
        sass: config.sass
    })

    grunt.loadNpmTasks('grunt-contrib-uglify');
    grunt.loadNpmTasks('grunt-contrib-sass');
};

// module.exports.GruntBootstrap = function (grunt) {
//     return grunt.initConfig(config);
// }